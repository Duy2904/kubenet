apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: tour-operation-react
  name: tour-operation-react
  namespace: uat
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tour-operation-react
  template:
    metadata:
      labels:
        app: tour-operation-react
    spec:
      containers:
      - image: tripotadevacr.azurecr.io/tour-operation-react-uat:tour-web-v1.0.0-12488
        imagePullPolicy: Always
        name: tour-operation-react
        ports:
        - containerPort: 80
      imagePullSecrets:
      - name: tripota-acr-secret
