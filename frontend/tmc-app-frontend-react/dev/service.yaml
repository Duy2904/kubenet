apiVersion: v1
kind: Service
metadata:
  name: tmc-app-frontend-react-svc
  namespace: development
spec:
  selector:
    app: tmc-app-frontend-react
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
