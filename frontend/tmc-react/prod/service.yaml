apiVersion: v1
kind: Service
metadata:
  name: tmc-react-svc
  namespace: prod
spec:
  selector:
    app: tmc-react
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
